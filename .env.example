# OpenRouteService — free tier, 2000 req/day
# Sign up at https://openrouteservice.org/dev/#/signup
# Then create a token and paste it below.
# Without this key the app falls back to OSRM (no height filtering).
VITE_ORS_API_KEY=your_key_here
